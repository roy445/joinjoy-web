services:
  - type: web
    name: joinjoy-web
    env: node
    branch: main
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm run start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
